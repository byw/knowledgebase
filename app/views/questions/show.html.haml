.question.details
  %h3.title=@question.title
  .info
    posted by
    %span.user=@question.user.name
    =time_ago_in_words @question.created_at
    ago
  .text=@question.text
  .tags
    %b Tags:
    -if @question.tags
      -@question.tags.each do |tag|
        %span.tag=tag

  %h3 Answers:
  -if @question.answers.empty?
    .no-answer no answers posted yet
  -else
    .answers
      -@question.answers.each do |answer|
        .answer
          .text=answer.text
          .info
            posted by
            %span.user=answer.answer_user.name
            =time_ago_in_words answer.created_at
            ago

  =form_for [@question, @new_answer] do |f|
    =f.text_area :text
    =f.submit 'Post an Answer'
